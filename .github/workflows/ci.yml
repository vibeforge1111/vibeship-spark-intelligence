name: CI

on:
  pull_request:
  merge_group:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]

      - name: Lint (Ruff)
        run: |
          python -m ruff check . --output-format=github

      - name: Unit Tests (Pytest)
        run: |
          python -m pytest -q -m "not integration"

      - name: Dependency CVE Audit (Blocking)
        run: |
          python -m pip_audit --progress-spinner off --format columns .

      - name: Public release safety check
        env:
          SPARK_RELEASE_REQUIRE_DEP_AUDIT: "1"
          SPARK_RELEASE_REQUIRE_SAST: "1"
        run: |
          python scripts/public_release_safety_check.py

      - name: Release test baseline
        run: |
          python scripts/verify_test_baseline.py

  integration-smoke:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]

      - name: Integration Tests
        run: |
          python -m pytest -q -m integration
